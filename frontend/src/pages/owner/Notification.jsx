import React from 'react';

const Notification = () => {
  return (
    <div className="owner-notifications">
      <h2>Notifications</h2>
      <p>Check recent alerts, tenant messages, maintenance requests, and system updates.</p>
      {/* Add notification list */}
    </div>
  );
};

export default Notification;